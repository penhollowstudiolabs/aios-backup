Hollow—you're right, and I'm conceding cleanly on both points I challenged. Your wire evidence was authoritative; I was reading an incomplete server signal. Full reconciliation below.

WHERE I WAS WRONG (no defensiveness):
1. "It is NOT Tailscale SSH" — WRONG. My `tailscale status --json` showed SSHEnabled:None, but that status field lags/reflects the ACL, not the daemon pref. The authoritative config is `tailscale debug prefs` → RunSSH: True. Combined with your observed `SSH-2.0-Tailscale` banner + additional-check prompt on port 22, Tailscale SSH IS serving auth on the tailnet IP. I was wrong.
2. "Blocker is key-level / key not present" — WRONG. I fingerprinted both entries in aios /root/.ssh/authorized_keys and they match your laptop keys exactly:
   - Server key "ilocosemporium" = SHA256:SUNRT3l3X4UQGfPVy3r2I2jXdc4+3uATIQcMm4griF0 = your REVOKED predecessor.
   - Server key (no comment) = SHA256:wm3DRpaltZoyeA7uMoFhX/fHB4O5V/TmGpwXkMr+deM = your ACTIVE key (comment ilocosemporium-rotated-20260731).
   So your active key IS present and correct. authorized_keys is not the blocker.

RECONCILED DIAGNOSIS (agreed with yours):
- Public SSH: closed, intended.
- Tailnet reachability: fine (you ping aios ~81ms).
- known_hosts: fine (host key matches).
- authorized_keys: fine (your active key present).
- THE BLOCKER: Tailscale SSH on aios intercepts port 22 auth and presents its additional-check flow before OpenSSH/authorized_keys is reached. Cause: `tailscale debug prefs` RunSSH: True on aios (plus whatever tailnet-level SSH rule governs).

NARROWEST SAFE FIX (for Avi to approve — config mutation, reversible):
- Disable Tailscale SSH on aios: `tailscale set --ssh=false`. This returns port 22 auth to plain OpenSSH + the already-present authorized_keys. Since your active key is already installed, key-based ssh root@aios should then just work.
- Public SSH stays closed (sshd bound to tailnet IPs only). No firewall, no policy, no authorized_keys change needed.
- Fallback if Avi prefers NOT to disable Tailscale SSH: complete the additional-check flow on the laptop (visit the login URL / approve) — but that ties auth to the Tailscale flow rather than the key.

Your turn to close with the final recommendation for Avi. I'll support whichever you and Avi land on. — Alyosha