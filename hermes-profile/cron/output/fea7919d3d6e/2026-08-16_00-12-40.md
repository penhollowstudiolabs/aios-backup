# Cron Job: check-openclaw-changelog

**Job ID:** fea7919d3d6e
**Run Time:** 2026-08-16 00:12:40
**Schedule:** 0 0 * * 0

## Prompt

[IMPORTANT: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

Check the OpenClaw GitHub repository for the latest changes in CHANGELOG.md. Report any new entries or significant updates since the last check.

## Response

I have everything I need. Baseline (Aug 9 check) → current diff complete. Here's the report.

---

**OpenClaw CHANGELOG — weekly check (Aug 16, 2026)**

No new *released* version landed — latest stable remains **v2026.7.1** (2026-08-04); current beta is **v2026.8.1-beta.2** (Aug 15). All new work is accumulating in the **Unreleased** section, which grew meaningfully this week.

**New in Unreleased → Changes (9 items since last check):**
- **Secret egress host binding** — binds each shared-store secret to exact HTTPS destinations (CLI, Gateway RPC, Control UI); unbound sentinel substitution fails closed before plaintext egress. *Security hardening.*
- **Release validation** — deferred beta Parallels smoke to postpublish, bounded nested workflow monitors with job timeouts.
- **macOS app profiles** — isolates named app instances across state/prefs/Keychain/Gateway while leaving host-global login untouched.
- **Control UI sidebar usage** — removed the provider usage quota row from the expanded sidebar (details still in composer + Usage page).
- **Android chat code highlighting** — themed syntax colors for Kotlin/Swift/TS/JS/Python/Bash/JSON.
- **Gateway TTS playback** — new operator-scoped `tts.speak` RPC returning speech as inline audio for remote clients.
- **Workboard dispatch cap** — added `--max-starts` request override.
- **Plugin install provenance warnings** — explicit `--force` now required for arbitrary executable plugin sources; Crestodian restricted to trusted sources. *Security.*
- **Custodian rich setup controls** — native selects/multiselects/masked secrets in the Control UI wizard.

**Fix backlog (328 items) — major clusters, most landed since Aug 9:**
- **Buzz (heavy, directly relevant to the weekend pilot that opened Aug 15):** plugin packaging/restructuring, lifecycle recovery, standalone sends, presence, bot profiles, agent identity, guided & resumable setup, inbound authorization, output sanitization. The Buzz lane is being actively hardened — the exact friction that parked it (8/5) is getting addressed.
- **ClickClack:** split-origin setup codes, durable media delivery, output sanitization.
- **Control UI:** session companion, staged attachments, browser annotations, deep links, owner handoff, archived-session deletion, operator session permissions, cloud session thinking, model readiness.
- **Telegram (relevant to Hollow's outage):** durable ingress, live-location hook exposure, reasoning previews, DM conversation context.
- **Codex/agents:** subagent fan-out settle, native subagent handoff/controls, yielding, `/status` freshness, resumed permissions, runtime switching.
- **macOS:** remote tunnel lifecycle, Voice Wake cancellation, keyboard navigation, SecretRefs.

**Fit for Avi's stack:** nothing urgent. Two watch-worthy threads: (1) the **Buzz hardening** validates revisiting that Lane-F pilot now that it's open; (2) two **security hardening** items (secret egress binding, plugin provenance) are the type to let the community vet before adopting. No action required — flagging for context.
