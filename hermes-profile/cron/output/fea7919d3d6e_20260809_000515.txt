Cronjob Response: check-openclaw-changelog
(job_id: fea7919d3d6e)
-------------

I have retrieved the `CHANGELOG.md` file from the OpenClaw GitHub repository.

Here are the new entries and significant updates since the last check (which, for this execution, means the entire content of the changelog as it is now):

**Unreleased Changes:**

*   **Developer workflow:** Removed the obsolete `scoped-commit` helper, now using standard Git commands in isolated worktrees.
*   **Plugin uninstall cleanup:** Improved the process of removing uninstalled plugin paths from configuration.
*   **Crabbox hydration:** Fixed issues with Crabbox hydration on unprivileged cloud sandboxes by falling back to a user-writable pnpm store and making Docker an explicit capability.
*   **Browser extension relay CDP compat:** Enhanced compatibility for controlling Chrome via Puppeteer and added `openclaw browser extension cdp` command.
*   **Local model setup:** Improved setup for Ollama, llama.cpp, and LM Studio, including retries and model verification.
*   **Control UI first-run setup:** Streamlined the initial setup process, including model verification and channel setup.
*   **Fish Audio speech:** Added hosted S2.1 synthesis with streaming, voice notes, discovery, and telephony, plus local Fish S2 Pro support.
*   **Control UI cloud workspace conflicts:** Improved display and handling of cloud workspace conflicts.
*   **Control UI update recovery:** The "new version available" button now correctly waits for gateway restarts.
*   **Control UI sender identity polish:** Improved display of sender avatars and names in chat.
*   **Control UI who's-online roster:** Added a scrollable roster of online users accessible from the sidebar footer.
*   **Discord and Slack native login:** Registered `/login` command menus.
*   **Control UI user profiles:** Users can now manage their display name and avatar.
*   **Trusted-proxy browser pairing:** Optional auto-approval of new devices from allowlisted proxy identities.
*   **Channel plugin ingress monitors:** Introduced a shared plugin SDK monitor for durable admission and lifecycle management.
*   **Dashboard MCP apps:** Pinned originating-session MCP app views as dashboard widgets.
*   **External gateway supervision:** Added `OPENCLAW_SUPERVISOR_MODE=external` for lifecycle owners.
*   **Buzz message fidelity and typing indicators:** Improved handling of messages and typing indicators in Buzz.
*   **Buzz sender directory:** Exposed directory entries from relay state.
*   **Buzz native mentions:** Resolved mentions into native `p` tags.
*   **ClickClack guided setup and command menus:** Enhanced setup and command menu integration for ClickClack.
*   **Skill Workshop approvals:** Defaulted to automatic apply/reject/quarantine actions, with an opt-in approval gate.
*   **TUI fuzzy selectors:** Delegated list matching to `pi-tui`.
*   **macOS paired-node terminals:** Improved resume commands and input forwarding for Codex and Claude terminals.
*   **Control UI catalog terminals:** Enhanced opening of Codex and Claude Code sessions.
*   **Control UI coding catalogs:** Display provider brand icons for coding sessions.
*   **Skill Workshop history review:** Added a manual session scan for skill ideas.
*   **SQLite snapshots:** Introduced `openclaw backup sqlite` for managing database artifacts.
*   **GPT-5.6 Ultra and runtime switching:** Added support for new models and improved runtime switching.
*   **OpenAI GPT-5.6 defaults:** Set new default models for OpenAI API and Codex/OAuth.
*   **Meta provider:** Added support for the `muse-spark-1.1` model.
*   **Android chat agent selector:** Enhanced agent selection directly from the chat screen.
*   **Gateway host status:** Displayed host and system details in Control UI Settings.
*   **iOS offline chat:** Improved offline chat experience with caching.
*   **Slack progress indicators:** Utilized native assistant thread status and loading messages.
*   **Control UI Talk controls:** Centralized voice and model settings.
*   **Control UI session workspace shortcut:** Added `⇧⌘B` to expand/collapse the session workspace.
*   **Control UI Settings shortcut:** Added `⇧⌘,` to open Settings.
*   **Control UI chat layout:** Improved chat view layout with centered transcript.
*   **Control UI composer footer:** Adjusted layout of chat settings and model controls.
*   **Control UI assistant actions:** Improved placement of assistant actions.
*   **Cron model selection:** Allow model selection for cron jobs.
*   **Control UI GitHub previews:** Enhanced previews for GitHub issues and pull requests.
*   **Logbook work journal:** Added a new plugin for a private timeline and Q&A.
*   **Control UI message context:** Displayed token and model details on hover.
*   **Control UI session titles:** Improved display of truncated session names.
*   **Control UI sidebar navigation:** Redesigned sidebar navigation with pinned destinations and footer settings.

**Version 2026.1.5:**

*   **Highlights:** Added image-specific model configuration, a new `image` tool, default model shorthands, and documentation for model precedence. Bun is now an optional local install/build workflow.
*   **Fixes:**
    *   Control UI improvements for Markdown rendering, Discord rule layouts, and chat view scrolling.
    *   Android: foreground service notification now brings the app to the front.
    *   Cron tool uses `id` for updates and runs.
    *   macOS: improved location permission handling, concurrency for models, and JSON5 config parsing.
    *   WhatsApp: suppressed typing indicators during heartbeats and marked history sync messages as read.
    *   Discord: avoided duplicate replies for streaming events.
    *   CLI: adjusted local gateway calls to use Tailscale IPs.
    *   Env: improved fallback loading for `.env` files.
    *   Agent tools: fixed OpenAI-compatible tool schemas.
    *   Onboarding: auto-builds missing Control UI assets when running from source.
    *   Discord/Slack: correctly routed notifications to sessions.
    *   Agent tools: honored `agent.tools` allow/deny policy.
    *   Discord: avoided duplicate replies for repeated `message_end` events.
    *   Commands: unified `/status` and command authorization.
    *   CLI: `openclaw agent` now defaults to running via Gateway.

The changelog is quite extensive. For brevity and focus on "significant updates," I've summarized the main points from the "Unreleased" section and the key highlights and fixes from the "2026.1.5" release. The full changelog content is available in the provided output and can be read using the `read_file` command if needed.

To stop or manage this job, send me a new message (e.g. "stop reminder check-openclaw-changelog").